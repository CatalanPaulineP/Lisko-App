name: background_sms
description: Background SMS plugin for LisKo.
version: 0.0.4

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.j.background_sms
        pluginClass: BackgroundSmsPlugin
      ios:
        pluginClass: BackgroundSmsPlugin
